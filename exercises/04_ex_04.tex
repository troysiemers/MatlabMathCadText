{In the same figure window, plot $y=\sin(t+\phi)$ for each of $\phi = 0, \pi/4$ and  $\pi/2$, on the range for $t$ from $0$ to $2\pi$.  Using the \cour{text} command, label the plots inside the figure window.  Also, use \cour{xlim}, \cour{ylim} to make the plots fit nicely in the plot window.}
{}